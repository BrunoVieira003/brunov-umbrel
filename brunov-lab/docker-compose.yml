version: '3'

services:
  api:
    image: python:3-alpine3.22
    working_dir: /app
    volumes:
      - ./app:/app
    ports:
      - "5000:5000"
    command: >
      bash -c "pip install flask && ls && pwd && python app.py"
